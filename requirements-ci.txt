# Minimal dependency set for CI. The full `requirements.txt` carries
# torch + transformers (~2 GB download, 5–10 min install) which our
# offline test suite doesn't exercise — the tests run entirely without
# them. If CI starts exercising ML code, move those deps here.

fastapi==0.115.0
pydantic==2.9.2
# Required by pydantic.EmailStr in api/auth_routes.py — CI tests now import
# that module through tests/test_auth_routes_import.py to catch regressions.
email-validator==2.2.0
aiohttp==3.10.5
# httpx — required by fastapi.testclient (starlette.TestClient) for
# tests/test_deputy_routes.py smoke tests against the FastAPI app.
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
