# ---- Core ----
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# ---- DB / async ----
asyncpg==0.29.0
SQLAlchemy==2.0.35
alembic==1.13.2

# ---- Redis / Celery ----
redis==5.0.8
celery==5.4.0

# ---- HTTP / parsing ----
aiohttp==3.10.5
beautifulsoup4==4.12.3
feedparser==6.0.11

# ---- Messaging / social ----
telethon==1.36.0

# ---- NLP (optional; pinned loosely) ----
transformers>=4.44
torch>=2.2 ; platform_system != "Windows"

# ---- Dev / tests ----
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
ruff==0.6.8
